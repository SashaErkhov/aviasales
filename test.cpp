#include <iostream>
//#include "Array.h"
//#include "AviaTickets.h"

int main()
{
	/*std::cout << "<";
	char* a=new char[10];
	std::cin.getline(a,10);
	std::cout << std::endl;
	for (int i = 0; i < 10; ++i)
	{
		std::cout << a[i] << "---" << (int)a[i] << std::endl;;
	}
	std::cout << std::endl;
	delete[]a;*/
	//Arry x;
	//Arry y(2);
	//y.setElement(0, 0);
	//y.setElement(1, 1);
	//std::cout << "y[0] = " << (int)y.getElement(0) << " y[1] = " << (int)y.getElement(1) << std::endl;;
	//y.addElement(2);
	//std::cout << "new y[2] = " << (int)y.getElement(2) << std::endl;
	/*char *a=new char[5];
	char b[3];
	while (true)
	{
		std::cout << "> ";
		std::cin.getline(a,5);
		for (int i = 0; i < 5; ++i)
		{
			std::cout << "a[" << i << "] = \"" << a[i] << "\" int = " << int(a[i]) << std::endl;
		}
	}
	delete[] a;*/
	/*unsigned char* str = new unsigned char[20];
	while (true)
	{
		std::cout << "> ";
		std::cin.getline((char*)(str), 20);
		for (int i = 0; i < 20; ++i)
		{
			std::cout << "str[" << i << "] = \"" << str[i] << "\" int = " << int(str[i]) << std::endl;
		}
		Arry command;
		for (int i = 0; i < 20; ++i)
		{
			if (!(str[i] == ' ' or str[i] == 0))
			{
				command.addElement(str[i]);
			}
			else
			{
				break;
			}
		}
		for (int i = 0; i < command.size; ++i)
		{
			std::cout << "command.m_bytes[" << i << "] = \"" << command.m_bytes[i] << "\" int = " << int(command.m_bytes[i]) << std::endl;
		}
	}
	delete[]str;*/
	//AviaTickets x;
	/*Arry x;
	x.addPhraze("Wrong date format: ", 19);
	for (int i = 0; i < x.size; ++i)
	{
		std::cout << "x[" << i << "] \"" << x.m_bytes[i] << "\" int = " << (int)x.m_bytes[i] << std::endl;
	}*/



	std::system("pause");
	return 0;
}